{
  "name": "@tensnap/go",
  "version": "0.3.0",
  "description": "Go WebSocket server implementing the TenSnap protocol v0.2 (simulator side)",
  "private": true,
  "scripts": {
    "build": "go build ./...",
    "lint": "go fmt ./...",
    "format": "go fmt ./...",
    "vet": "go vet ./...",
    "test": "go test ./...",
    "test:race": "go test -race ./...",
    "test:cover": "go test -coverprofile=coverage.out ./... && go tool cover -html=coverage.out -o coverage.html",
    "tidy": "go mod tidy",
    "generate": "go generate ./...",
    "check": "pnpm vet && pnpm lint && pnpm test && pnpm check:examples"
  }
}
