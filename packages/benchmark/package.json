{
  "name": "@tensnap/benchmark",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "benchmark": "vite",
    "build": "tsc && vite build",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@preact/signals": "^2.9.0",
    "@tensnap/core": "workspace:*",
    "@tensnap/web-adapter": "workspace:*",
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.8"
  }
}