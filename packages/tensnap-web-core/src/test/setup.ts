// Jest setup for tensnap-web-core
// This file runs before all tests

// Mock DOM APIs if needed
if (typeof window !== 'undefined') {
  // Setup window mocks
}

// Global test utilities can be added here
global.beforeEach(() => {
  // Reset state before each test
});
